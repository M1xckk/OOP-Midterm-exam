 import { Playlist } from "./Playlist";
 import { Album } from "./Album";
 //import { PlaylistImporter } from "./PlaylistImporter";
 
 //export class and implements sth {
      //private _path: string; 
    
      // constructor(path: string) {
      //   this._path = path;
      // }
    
      // public loadPlaylist(): Playlist { can not fix return a value error
      //   console.log(`Your playlist at location ${this._path} will be loaded`);
      // }
        // Load the JSON data from file and transform it into a Playlist object
        //const the playlist
        // use for loop to complete it. 
        //const the album
        //use for loop to complete it. 

    